<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.yangyouwang.module.wx.mapper.RedPacketBatchMapper">
    <resultMap id="BaseResultMap" type="io.github.yangyouwang.module.wx.entity.RedPacketBatch">
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="batch_id" jdbcType="VARCHAR" property="batchId" />
        <result column="total_amount" jdbcType="DECIMAL" property="totalAmount" />
        <result column="total_count" jdbcType="INTEGER" property="totalCount" />
        <result column="used_count" jdbcType="INTEGER" property="usedCount" />
        <result column="withdrawn_count" jdbcType="INTEGER" property="withdrawnCount" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        id, batch_id, total_amount, total_count, used_count, withdrawn_count, status, create_time, update_time
    </sql>

    <select id="selectByBatchId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from red_packet_batch
        where batch_id = #{batchId}
    </select>

    <update id="incrementWithdrawnCount">
        update red_packet_batch
        set withdrawn_count = withdrawn_count + 1
        where batch_id = #{batchId}
    </update>

    <update id="incrementUsedCount">
        update red_packet_batch
        set used_count = used_count + 1
        where batch_id = #{batchId}
    </update>
</mapper>
